import React, { CSSProperties } from "react";

interface PropsI {}
const MetaPanel = ({}: PropsI) => {
  return (
    <div style={styles}>
      <h1>MetaPanel</h1>
    </div>
  );
};
export default MetaPanel;

const styles: CSSProperties = {};
